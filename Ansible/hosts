all:
  children:
    server:
      hosts: # server グループのホスト
        target_server:
          ansible_host: "{{ target_ip }}"
      vars:
        ansible_user: 'admin'
        ansible_ssh_private_key_file: '/Users/tanino/AWS/AutoScalingVerification/Terraform/files/ssh/ssh_rsa_key'